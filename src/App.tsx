import { RenderView } from "./components";

import MainNavigator from "./navigation/MainNavigator";

function App() {
  return (
    <RenderView>
      <MainNavigator />
    </RenderView>
  );
}

export default App;
